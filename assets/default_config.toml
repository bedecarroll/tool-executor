provider = "codex"
search_mode = "first_prompt"
# preview_filter = "glow -s dark" # Optional: pipe TUI preview through an external command.

[providers.codex]
bin = "codex"
flags = ["--search"]
# Session logs are discovered automatically under $CODEX_HOME (defaults to ~/.codex).
